#!/usr/bin/env zunit

@test 'Testing condition lacking consequents'  {
	load ../agkozak-zsh-theme.plugin.zsh
	zpml

	FOO=( if is_superuser then
		else
		fi
	)

	assert "$(zpml compile FOO)" same_as '%(!..)'
}

# vim: ts=2:sts=2:sw=2
