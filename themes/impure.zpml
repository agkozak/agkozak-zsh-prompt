# impure prompt
#
# Based loosely on the pure prompt: https://github.com/sindresorhus/pure
#
# https://github.com/agkozak/agkozak-zsh-theme

AGKOZAK_BLANK_LINES=1

ZPML_PROMPT=(
  fg_blue literal '%~' unfg

	if is_git then
		space fg_242 literal '(' git_branch_status literal '%)' unfg
	fi
  newline
  if is_exit_0 then
    fg_magenta
  else
    fg_red
  fi
    literal '❯' unfg space
)

ZPML_RPROMPT=()

# vim: ft=zsh:ts=2:sts=2:sw=2
